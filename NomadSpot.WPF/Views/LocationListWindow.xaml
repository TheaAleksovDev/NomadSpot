<Window x:Class="NomadSpot.WPF.Views.LocationListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Locations" Height="500" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Columns:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <CheckBox x:Name="ColName" Content="Name" IsChecked="True" Margin="5,0" Checked="Column_Changed" Unchecked="Column_Changed"/>
            <CheckBox x:Name="ColAddress" Content="Address" IsChecked="True" Margin="5,0" Checked="Column_Changed" Unchecked="Column_Changed"/>
            <CheckBox x:Name="ColRating" Content="Rating" IsChecked="True" Margin="5,0" Checked="Column_Changed" Unchecked="Column_Changed"/>
            <CheckBox x:Name="ColNoise" Content="Noise Level" Margin="5,0" Checked="Column_Changed" Unchecked="Column_Changed"/>
            <CheckBox x:Name="ColWifi" Content="WiFi" Margin="5,0" Checked="Column_Changed" Unchecked="Column_Changed"/>
        </StackPanel>

        <DataGrid x:Name="LocationsGrid" Grid.Row="1" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="LocationsGrid_SelectionChanged">
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Add Review" Margin="0,0,10,0" Padding="10,5" Click="AddReview_Click"/>
            <Button Content="View Reviews" Margin="0,0,10,0" Padding="10,5" Click="ViewReviews_Click"/>
            <Button Content="Mark Inactive" Margin="0,0,10,0" Padding="10,5" Click="MarkInactive_Click"/>
            <Button Content="Clear" Padding="10,5" Click="Clear_Click"/>
        </StackPanel>
    </Grid>
</Window>